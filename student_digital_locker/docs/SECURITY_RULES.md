# Legacy Flask Security

This folder is a legacy Flask/SQLite copy and is not the active GitHub Pages deployment.

Use the root Supabase files for the active project:

- `../supabase/schema.sql`
- `../supabase/rls-policies.sql`
